package Omnibase;
/*
 * This interface is a set of common CRUD applications expected in all records.
 * Records are responsible for all CRUD actions on Data.
 * Records are implemented differently for different database applications. Mongo records are documents, while SQL records are rows in a table.
 * Records exist within a table and assume the existence of a table.
 */
 interface IRecord
{

}

